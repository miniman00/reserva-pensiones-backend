package uy.pensiones.enums;

/** Estado administrativo calculado usando status + startsAt/endsAt. */
public enum PensionPromotionEffectiveStatus {
    SCHEDULED,
    ACTIVE,
    EXPIRED,
    CANCELLED
}
